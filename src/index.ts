let message: string;

message = 'Hello World!';

if (message.length > 0) {
	console.log(message); // this is a very long line to see where line wrap happens on save to try to confirm if the formatter is honoring my settings.
}
